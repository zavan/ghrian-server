# Singleton broker settings. Password omitted on purpose: the column is encrypted,
# and fixtures bypass encryption, so a plaintext value here would fail to decrypt.
primary:
  host: localhost
  port: 1883
  username: ""
  client_id: ""
  base_topic: ghrian
  use_tls: false
